{% extends "base.html.twig" %}

{% block body %}
	<div class="container">
		<h1 class="my-4 text-center">New Program</h1>
		<div class="row">
			<div class="col-md-8 mx-auto">
				{{ form_start(form) }}
				{{ form_widget(form) }}
				<div class="d-grid">
					<button class="btn btn-outline-secondary" type="submit" formnovalidate>
						Add program
					</button>
				</div>
				{{ form_end(form) }}

			</div>
		</div>
	</div>

{% endblock %}